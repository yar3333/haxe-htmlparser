#include "HtmlDocument.h"

/*void HtmlDocument::HtmlDocument(string str)
	:HtmlElement("", vector<HtmlNodeElement>)
{
	auto nodes = HtmlParser.parse(str);
	for (node in nodes)
	{
		addChild(node);
	}
}*/

