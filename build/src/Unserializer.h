#ifndef UNSERIALIZER_H
#define UNSERIALIZER_H

#include "common.h"
/*
class Unserializer
{
	static char BASE64[];

	static map<int,int> CODES;

 	string buf;
 	int pos;
 	int length;
 	vector<void *> cache;
 	vector<string> scache;

 	public: Unserializer(string buf);

	inline int get(int p) { return buf[p]; }

 	int readDigits();

	void unserializeObject(void *o);

 	public: ISerializable *unserialize();

	public: static void *run(string v);
};*/

#endif